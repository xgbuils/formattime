Required `grunt-cli` & `grunt-init` global installation:
``` 
$ git clone https://github.com/xgbuils/formattime
$ npm install
```

Install:
``` 
$ git clone https://github.com/xgbuils/formattime
$ npm install
```

Test:
`$ grunt`
or
`$ grunt test`